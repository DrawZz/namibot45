const Discord = require("discord.js");

var bot = new Discord.Client();

bot.on("ready", function() {
bot.user.setGame("NamiBot,.nHelp");
console.log("Nami est connecté !");
});

bot.login(process.env.TOKEN)
